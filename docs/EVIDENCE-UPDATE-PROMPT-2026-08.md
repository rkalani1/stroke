# Copy/paste prompt — evidence & quality update (everything EXCEPT the Example Protocols section)

Generated 2026-08-22 from a six-track audit of commit `20c702f`. Paste everything below the horizontal rule into a fresh Claude Code session opened on `rkalani1/stroke`.

---

You are updating **rkalani1/stroke**, a client-side React PWA served by GitHub Pages straight from `main`'s repo root (build artifacts are committed). It is used clinically as a stroke decision-support and reference tool, so factual accuracy against best available evidence is the top priority. Your mission: bring every surface EXCEPT the Example Protocols section up to current best evidence, fix the verified defects listed below, reconcile internally contradictory clinical statements, and leave the validation suite green.

This worklist comes from a full audit performed 2026-08-22 at commit `20c702f` (branch `claude/git-page-review-prompt-28cnal`, then 8 commits ahead of `origin/main`, containing the v6.20.0 Trials rebuild). Before starting, run `git branch -a --contains 20c702f` and base your work on the newest branch that contains that commit (use `main` if it has since absorbed it; otherwise branch from `claude/git-page-review-prompt-28cnal`). **Line numbers below were true at `20c702f` — always re-locate by the quoted identifier or string, never trust a bare line number.**

## 0. Non-negotiable ground rules

### 0a. The frozen Example Protocols zone — do not change what it renders

- The rendered text of `#/protocols/ich` and `#/protocols/ischemic` is locked byte-for-byte by `scripts/snapshot-example-protocols.mjs` against baselines in `tests/snapshots/example-protocols/{ich.txt, ischemic.txt}`. Those two baselines must be **byte-identical** at the end of your work. Never run `npm run test:protocol-snapshot:update` to absorb a change to them.
- Frozen source regions in `src/app.jsx` (at `20c702f`): the Protocols tabpanel `activeTab === 'protocols'` (≈28010–30103, `<div id="tabpanel-protocols">`), plus everything whose text renders inside it: `protocolDetailMap` (≈2973–3087), `src/institutional-protocols.js` (`ICH_INITIAL_EVALUATION_ALGORITHM`), `src/management-guidance.js` (`AIS_COMMAND_CENTER_CARDS`, `AIS_SOURCE_LINKS`), `src/pocket-cards.jsx`. Do not edit any rendered string in these; className-only fixes are technically snapshot-safe but treat the whole region as read-only unless an item below explicitly says otherwise.
- The snapshot also captures the calculators panel (`#research-tabpanel-calculators`, route `#/research/calculators`, baseline `calculators.txt`). That panel is **in scope** (it is the Research tab, not Protocols), but any rendered-text change there requires re-baselining `calculators.txt` via `npm run test:protocol-snapshot:update` in a **separate, clearly-labeled commit** whose diff touches only `calculators.txt` — and you must confirm `ich.txt` and `ischemic.txt` show zero diff in that run.
- The snapshot harness renders the **built `app.js`**, not `src/app.jsx` — always rebuild before running it.

### 0b. Tests that lock exact source strings — keep them satisfied

- `tests/navigation-reorganization.test.js` slices `src/app.jsx` between the literal strings `const MANAGEMENT_SUBTABS` and `const AutoDetectedContraindicationsBanner` and evaluates the slice with `new Function()`. Everything between those two markers must stay self-contained (no imports/hooks/outer identifiers), keep exporting the same symbols, and neither marker string may appear anywhere else. It also asserts exact substrings elsewhere (`const tabs = ['encounter', 'protocols', 'trials', 'research'];`, the `aria-label="Main sections"` nav block, keyboard map `'1'..'5'`). Read that test before touching navigation code.
- `tests/clinical-corrections.test.js` requires exact corrected phrases and bans the old wrong ones in specific files (THALES/CHANCE-2 durations in `src/app.jsx`; `case 4: return "97%"` in `src/education.jsx`; the HINTS wording in `src/teaching.js`; TREAT-CAD wording in `src/guidelines/landmark-trials.json` and `src/components.jsx`; `Osm Gap &gt; 20` in `src/simulators/EvdIcpSimulator.jsx`). When you edit near these, keep the required phrases intact.
- `tests/protocol-currency-guards.test.js` scans 13 files **including the built `app.js`** for required/banned phrasings (service-worker update path, no US-phone patterns, pinned PMIDs in `data/atlas/*`). Rebuild after edits or it goes split-brain.

### 0c. Clinical edit doctrine

- **Verify before you write.** Every clinical number, threshold, COR/LOE, DOI, PMID, or trial result you touch must be re-verified against a primary source (PubMed, the journal page, ClinicalTrials.gov, the guideline PDF) in this session — including the claims in this prompt. If you cannot verify something, do not change it; list it in the PR as unresolved.
- **Cite what you write.** New or corrected clinical claims get a PMID/DOI in the appropriate registry (`src/evidence/citations.js` + `citationIds`, or the rule's `reference`/`sourceUrl`).
- **Conservative direction.** Where evidence is genuinely contested, prefer the framing already used by the app's best modules: state the trial result in the direction it was found, name the population, quote the harm signal, and avoid COR/LOE labels no guideline issued.
- **Judgment calls go to the PR, not silently into code.** Items marked *(judgment)* below: implement the recommended option but call it out in a "Decisions for physician review" section of the PR description.
- Content domains `content/guidelines/*` and `content/trials/*` are **projections** — edit `src/evidence/recommendations.js` / `src/evidence/completedTrials.js` and run `npm run content:seed`; never hand-edit those two directories.
- Obey `COMPLIANCE.md` and the public-demo guardrails (`src/public-demo-guardrails.js`): no PHI, no institutional identifiers, no phone-number-shaped strings anywhere, keep every disclaimer.

### 0d. Required workflow

Setup: `npm ci && npx playwright install chromium && npm run hooks:install`.

After each logical group of edits:

```bash
npm run content:validate && npm run content:seed:check && npm run content:bundle:check
npm run evidence:validate && npm run validate:citations && npm run validate:pmids && npm run validate:inline-citations
npm run build                      # regenerates content bundle, agent assets (llms.txt, sitemap, data/*), css, app.js
npm run test:protocol-snapshot     # ich.txt + ischemic.txt must be unchanged
npm run test:unit
npm run check:leak-guard
```

Ship-ready checklist: bump the version in **all five lockstep locations** (`package.json`, `service-worker.js` `APP_VERSION` + `CACHE_NAME` (dots→dashes, keep the exact two-space alignment), `index.html` asset `?v=` query strings ×3, `index.html` inline `APP_VERSION`) plus the hardcoded expectations inside `tests/service-worker.test.js`; add any newly served file to `CORE_ASSETS` in `service-worker.js`; re-run `npm run build` so `_meta.appVersion` regenerates; add a `CHANGELOG.md` entry (it is currently 9 versions behind — see P4-10); commit the rebuilt `app.js`/`tailwind.css`/`data/**`/`llms*.txt` — the live-smoke workflow sha256-compares the deployed Pages artifacts daily and opens an alert issue on drift.

Work in reviewable commits ordered P0 → P1 → P2 → …; open a **draft PR** with a "Decisions for physician review" section and a status list of anything not completed. Complete P0–P2 fully before moving on; P3–P5 as capacity allows.

## 1. Current-evidence landscape (verify each item via web/PubMed before use)

The audit verified these externally on 2026-08-22; re-verify, since more may have published since:

- **2026 AHA/ASA AIS guideline** (DOI 10.1161/STR.0000000000000513, PMID 41582814) is in the app — but a **7-page correction/erratum** published mid-2026 (*Stroke* 2026;57(8):e461–e467, PMID 42507797, DOI 10.1161/STR.0000000000000530) is **not** reflected. There is also a high-profile "Proposed Corrections" commentary from the perfusion-imaging trialists (PMID 42535283). Diff the erratum against `src/guidelines/ais-2026.json` (195 recs) and apply every changed COR/LOE/wording.
- **CREST-2** (NEJM, DOI 10.1056/NEJMoa2508800, published Nov 2025): CAS+IMM significantly reduced 4-yr stroke/death vs IMM alone (2.8% vs 6.0%, P=0.02); **CEA+IMM did not** (3.7% vs 5.3%, P=0.24).
- **ATLAS** large-core IPD meta-analysis (Lancet 2026;407:2015-26, PMID 42107392): benefit consistent across ASPECTS/core strata; the one attenuated stratum is **core ≥150 mL beyond 6 h**; mortality aRR 0.82 (0.70–0.97); no sICH excess vs medical management.
- **OCEANIC-STROKE** (asundexian, NEJM 2026, DOI 10.1056/NEJMoa2513880): positive — reduced ischemic stroke on top of antiplatelets without increased major bleeding.
- **CAPTIVA**: NIH DSMB **halted the low-dose rivaroxaban arm July 2026** (safety + futility); ticagrelor arm continues; registry status ACTIVE_NOT_RECRUITING.
- ISC 2026 late-breakers: **CHOICE-2** (IA alteplase post-EVT improved mRS but 90-day mortality 12.1% vs 6.4%, P=.03; LVO population), **OPTION** (IVT to 24 h in non-LVO stroke, positive; JAMA 2026, DOI 10.1001/jama.2026.0210), **FASTEST** (rFVIIa in hyperacute ICH: curbs expansion, adds thrombotic risk), ATTENTION-LATE / TNK-PLUS (negative TNK-before/with-late-EVT), EMBOLISE (MMA embolization for SDH).
- ESOC 2026 late-breakers: TAPIS, ATTRACTION, INSTANT, ATILA (early antiplatelet strategies), HOPE, CHILL-ART (IA hypothermia during EVT, positive), plus OCEANIC-STROKE secondary analyses.
- **TEMPO-2** (TNK in minor non-disabling stroke with occlusion): negative, 90-day death 5% vs 1%.
- **2025 AHA/ACC hypertension guideline** (DOI 10.1161/HYP.0000000000000249): &lt;130/80 goal for all adults — the modern attribution for the app's secondary-prevention BP target.
- Trials-tab registry checks done at audit time: STEP, TESTED, SATURN, VERIFY, MINUTE, INTERCEPT, CAPPRICORN-1, SCOUTS-3, MR-PICS, TELE-REHAB-2 all RECRUITING and accurate; MOST completed (correct in prose, broken in metadata — see P0-4).

## 2. P0 — patient-safety fixes (do these first)

1. **Encounter EVT auto-engine treats unassessed ASPECTS as large core and never checks for an LVO.** In `src/app.jsx` (search `Early window large core (SVIN 2025)`, ≈20899–20960): `aspects` is `null` when unassessed, and `null >= 0 && null <= 5` is `true`, so a patient with no ASPECTS renders "CONSIDER — Early window large core" with a blank score; the engine also returns "EVT: CONSIDER" for any NIHSS ≥6 within 24 h with **no vessel-occlusion gate**, and the 4.5–9 h branch returns `eligible: true` with no perfusion gate; `telestrokeNote.nihssDetails` free-text presence is treated as an eligibility criterion (search `nihss >= 4 || telestrokeNote.nihssDetails`). Fix: require a documented LVO for EVT prompts, require numeric ASPECTS before any large-core branch, gate 4.5–9 h IVT on the existing `getWakeUpEligibilityForNote` perfusion logic, and drop the free-text condition. Prefer wiring the already-imported, better-gated evaluators (`evaluateLargeCoreEVT`, `recommendLateWindowLytic` in `src/calculators-extended.js`) instead of patching the inline heuristics.
2. **Symptomatic carotid patients are shown the asymptomatic CREST-2 "medical management alone" card by default.** `carotid_symptomatic` (search `carotid_symptomatic`) requires an explicit `telestrokeNote.carotidManagement.symptomatic` flag while `carotid_asymptomatic` fires on `!cm.symptomatic`, which defaults false. Derive symptomatic status from the encounter itself (ischemic stroke/TIA + ipsilateral stenosis) or make the asymptomatic card require an explicit "asymptomatic" selection.
3. **4F-PCC dosing is stated three incompatible ways in the Encounter tab.** `protocolDetailMap.PCC` says fixed "Kcentra 2000 units — not weight- or INR-tiered" (frozen text — do not edit); `reversal_warfarin.medications` says "25-50 IU/kg based on INR per AHA/ASA 2022 (max 5000 IU)"; `CLINICAL_PEARLS.pcc` says weight-based. *(judgment)* Recommended: label the Encounter-side texts explicitly ("guideline weight-based dosing; the Example Protocols tab shows a fixed-dose institutional example") so the two schemes can no longer be read as the same order; do not alter the frozen string.
4. **Trials matcher can print "ELIGIBLE" into clinical notes for closed/withdrawn trials.** (a) `src/evidence/activeTrials.js` RHAPSODY record: `nctId 'NCT04953325'` is a **Sinovac COVID-19 vaccine trial**; the real phase-3 3K3A-APC trial (NCT05484154) is **WITHDRAWN** — remove RHAPSODY from active matching. (b) `src/evidence/schema.js`: `ACTIVE_TRIAL_STATUS_VALUES` lacks `'completed'`, so MOST's `status: 'completed'` is silently coerced to `'recruiting'` — extend the enum with `completed/withdrawn/terminated/suspended`, make `makeActiveTrial` throw on unknown status, and add a status gate in `evaluateAllTrialsViaEngine` (`src/evidence/matcher-engine.js`) so non-recruiting trials are never matched or written into notes. (c) CAPTIVA record: status → active-not-recruiting; qualifying-event window is **30 days** (not 21); mRS gate is **≤4** (not ≤3); note the rivaroxaban-arm halt. (d) ASPIRE record: delete the invented `CHA₂DS₂-VASc ≥2` and `mRS ≤4` criteria (registry has neither). (e) STEP `fullName` → "StrokeNet Thrombectomy Endovascular Platform"; PICASSO `fullName` is wrong and it is missing the "ineligible for/failed IV tPA" hard gate. Add a reconciliation test asserting every acronym shared by `activeTrials.js`, `screenerTrials.json`, and `eligibilityTables.js` carries the same NCT, status, and window.
5. **EVD simulator drip model has a sign error that inverts its flagship teaching scenario.** `src/simulators/EvdIcpSimulator.jsx`, `computeDripInterval`: `trueMeanICP - (evdHeight + transducerOffset) * CMH2O_TO_MMHG` must be `evdHeight - transducerOffset` (offset is positive when the system sits **below** the tragus). Add a regression test with nonzero `transducerOffset` asserting the `overdrain` scenario drips faster than the corrected state. (`computeMeasuredICP`'s `+ transducerOffset` is correct — leave it.)
6. **Calculator formula errors** (all in `src/calculators-extended.js` unless noted; update the matching tests, which currently codify the wrong specs):
   - `calculateSPI2`: hypertension is weighted 3 (published: **1**); **CHF (3 pts)** and **index event = stroke vs TIA (2 pts)** are missing entirely (Kernan, Stroke 2000). Rebuild to the 7-item 0–15 score.
   - The "9-point ICH expansion score" (search `nine-point`/`calculateNinePoint`) is **not** the Brouwers 2014 score — it omits the CTA spot sign and adds items the score doesn't have; the citation also names the wrong journal. Rebuild to Brouwers (warfarin, spot sign, time-to-CT ≤6 h, volume tiers &lt;30/30–60/&gt;60 mL) or remove.
   - `calculateEDEMAScore`: zero overlap with the published EDEMA score (Ong, Stroke 2017: cistern effacement 3, glucose ≥150 2, no tPA/EVT 1, midline-shift tiers, no-prior-stroke 1) and cites a paper that defines no score. Rebuild or remove.
   - Boston criteria v2.0 (`isOver55` / `evaluateBostonCAA20`): age gate must be **≥50**, and **≥2 strictly lobar hemorrhagic lesions = Probable CAA** even without lobar ICH (currently under-called to "Possible", which changes anticoagulation conversations); the quoted "88% sensitivity" is not the published figure (~74.5% vs autopsy).
   - BAT score: dichotomize at **≥3** (currently ≥2) and fix the citation (Morotti, Stroke 2018, PMID 29669875 — not JAMA Neurol 2016).
   - SeLECT: the "L" item is **large-artery atherosclerotic etiology**, not LVO — rename `lvoArtery` and its label; re-verify the top rows of the risk table (score 9 published as ~63%/83%).
   - Boundary fixes: ESSEN age exactly 75 should score 1 (not 2); BRAIN volume exactly 10 mL should score 5 (not 0); VASOGRADE WFNS 3 + mFisher 1–2 is unclassified in the source (don't return Yellow); Ogilvy-Carter `posteriorOrGiant` should be giant **and** posterior; the glucose unit heuristic `glu > 30 ? glu/18 : glu` misreads severe hypoglycemia in mg/dL — use an explicit unit toggle.
   - ABCD² UI (`src/app.jsx`, search `Exactly 60 minutes`): the source defines ≥60 min = 2 points; remove the un-scoreable "exactly 60" option.
   - HAS-BLED UI (search `calc-hasbled`): add the published item definitions (uncontrolled SBP &gt;160; Cr &gt;2.26 mg/dL/dialysis/transplant; bilirubin &gt;2× + enzymes &gt;3×; age **&gt;65**; labile INR = TTR &lt;60%) — bare letter-words over-count nearly every patient.
7. **ICH-score mortality table is unsourced and non-monotonic.** `src/app.jsx` (search `cohortMortality`): `{0:'0.9%', 1:'0.7%', …}` — score 0 &gt; score 1, no score-6 row, no citation, untraceable to any publication. Replace with Hemphill 2001 30-day mortality (0/13/26/72/97/100%) plus the existing early-DNR caveat, or another explicitly cited cohort; add the citation to the card.
8. **HINTS misquote.** `src/app.jsx` (search `96.8`): "Sensitivity 96.8%, specificity 98.5% (Kattah 2009)" matches no source — Kattah 2009 reported 100%/96%. Fix the numbers, and add the two prerequisites (continuous AVS **with** nystagmus; not valid for episodic/positional vertigo). Do not touch the locked HINTS phrase in `src/teaching.js`.
9. **Relative contraindications hard-block TNK while the banner says "use clinical judgment."** `src/app.jsx` TNK auto-block `useEffect` (search `tnkAutoBlocked`) blocks on `recentSurgery`, `elevatedAPTT`, `lowGlucose` — all listed as *relative* in the checklist — and aPTT &gt;40 s is simultaneously labeled absolute, relative, and critical in four places. *(judgment)* Make the tiers consistent: absolute → hard block; relative → prominent warning requiring explicit override; label any deliberately conservative hard block as such.

## 3. P1 — evidence-currency updates

1. **Broken/wrong trial DOIs in `GUIDELINE_URLS`** (`src/app.jsx`, search `'TRACE-III':`): TRACE-III must be `10.1056/NEJMoa2402980`; OPTION must be `10.1001/jama.2026.0210` (and the OPTION rule's "N=570" → 566 analyzed). **Re-verify every DOI in the trial block** — two of ten sampled were wrong.
2. **Rewrite the `evt_large_core_early` rule** (search `ATLAS Trial 2026`): ATLAS is an IPD **meta-analysis**, not a trial; the claim that core estimation is "no longer strictly required for ASPECTS 0-2 in the late window" inverts ATLAS's actual caveat (limited evidence for **core ≥150 mL beyond 6 h**); add the mortality benefit, real citation (PMID 42107392) and a working `sourceUrl`; drop the self-assigned COR I/LOE A (no guideline issued it — the 2026 guideline's search closed before ATLAS); align the age caveat with `evaluateLargeCoreEVT` (18–85).
3. **`oceanic_stroke_fxia` rule**: currently future-tense with no result; update to the published positive NEJM 2026 result with citation, and loosen the unreachable `antiplateletRegimen === 'factor-xia-asundexian'` condition so the card can actually surface.
4. **`carotid_asymptomatic` rule**: present CREST-2 faithfully — significant stenting result, neutral CEA result — rather than "intensive medical management alone is the primary approach" with stenting as a "modest" footnote.
5. **Missing 2025–2026 evidence in the Encounter tab** (0 hits at audit): TEMPO-2 (add to the minor-stroke Class III context), ZODIAC (app hard-codes HOB 30° in four places — add the 0° pre-EVT evidence discussion), INTERACT4, RESCUE-BT2/tirofiban (wire the existing `adjunctiveAntithromboticAdvisory`), CHOICE-2 (wire `calculateAdjunctiveIAAlteplase`, imported but never called — include the mortality signal), MIND (add to `ich_mis_evac`), FASTEST, TAPIS, DISTAL 12-month outcomes, DIRECT-ANGIO (`direct_to_angio` still `classOfRec: 'N/A'`).
6. **COR/LOE regrades in `GUIDELINE_RECOMMENDATIONS`** (verify each against the guideline text): `bp_ischemic_no_lysis` I→IIb (and remove the `!inWindow` condition that leaves early non-lysis patients with no BP guidance); `permissive_hypertension` III/A is not an AHA grade and must exclude lysis/EVT candidates; `pfo_closure` I/A→IIa/B-R + add the nonlacunar/no-other-cause requirement; `carotid_symptomatic` 50–69% is IIa not I; `dapt_auto_stop` LOE A→B-R (III: Harm); `vte_ipc_admission` I/A→I/B-R and `vte_enoxaparin_timing` I/A→IIb; `sicas_dapt` and `esus_monitoring` mis-apply ACC/AHA COR to AAN/ECDP documents — re-attribute; `glp1ra_secondary_prevention` "Class I" for semaglutide has no guideline basis; `mevo_evt_not_recommended` body text says III/A while metadata says III/B-R — reconcile, and re-verify the class against the 2026 guideline + neutral (not harm) DISTAL/ESCAPE-MeVO results; `laao_after_ich` and `ich_anticoag_resumption` cite "AHA/ASA Secondary Prevention 2024 Update" and bare extreme HRs — fix attribution and add CIs.
7. **Guidelines library** (`src/guidelines/*.json`, mirrored to `data/guidelines/` by the build):
   - Apply the 2026 AIS guideline erratum (P1 item in §1) to `ais-2026.json`; add a `correctedBy`/`supersededBy` field to the schema and attach the published corrections for `poststroke-spasticity-2026`, `aha-creating-virtual-networks-2025`, `aha-care-ais-evt-icu-2021`.
   - Fix the two records whose text is literally `[Abstract not available]` (`aan-neuropalliative-2022.json`, `aha-recommendations-regional-destination-2021.json`).
   - Backfill the 17 missing PMIDs on the graded datasets (ais-2026 = 41582814, ich-2022 = 35579034, sah-2023 = 37212182, secondary-prevention-2021 = 34024117, cvt-2024 = 38284265, svin-large-core-2025 = 41573174, etc.).
   - Move the "June 2026 operational overlay" local-policy sentence out of `ich-2022.json` rec #89 (it attributes local algorithm text to AHA/ASA).
   - Add the missing AHA "Brain Health Across the Life Span" 2026 statement (PMID 42047038); normalize `No Benefit`/`No benefit` classNote casing; regenerate the 33 mid-word-truncated `shortTitle` values with word-boundary truncation; exclude `landmark-trials` from `data/guidelines/index.json`.
8. **teaching.js reconciliation** (`src/teaching.js` — keep the locked HINTS and THALES phrases intact): header + rules still anchored to the 2019 AIS guideline → 2026; ICH BP target pearl says Class 2a → **2b**; the ASPECTS &lt;3 pearl cites SELECT2/ANGEL-ASPECT (which enrolled 3–5) → LASTE/TESLA; post-EVT BP pearl attributes "Class 3: Harm" to BP-TARGET and BEST-II (neutral/futility trials) → attribute the class to the 2026 guideline per ENCHANTED2-MT/OPTIMAL-BP; DVT-prophylaxis window "day 2-4" → 24–48 h per the cited guideline; wake-up pearl says "alteplase is the evidence-based agent" while the pocket cards dispense TNK → state both with the TWIST/wake-up evidence nuance; add a caveat to the routine thrombophilia-panel pearl (low yield; protein C/S/AT uninterpretable acutely/on anticoagulation).
9. **landmark-trials.json**: add the ~15 missing 2024–2026 trials the education modules already cite (LASTE, TESLA, TENSION, DISTAL, ESCAPE-MeVO, SWITCH, TRIDENT, FASTEST, BASIS, CHOICE-2, TEMPO-2, TRACE-III, ANGEL-TNK, CAPTIVA, PROST). Preserve the locked TREAT-CAD phrase.
10. **Education modules** (`src/education.jsx` + regenerate `content/education/*.md`): update the CAPTIVA sentence (search `results pending`) for the July 2026 rivaroxaban-arm halt; rewrite the two ATLAS-superseded claims in `large-core-thrombectomy` (ASPECTS 0-2 → core ≥150 mL beyond 6 h; sICH framing; add mortality aRR 0.82) — the reference is already in the module's list; add CHOICE-2's mortality signal to the DMVO at-a-glance panel plus a note that CHOICE-2 enrolled LVO, not MeVO; qualify the `dmvo-mevo-management` claim that TNK is first-line "for all eligible patients" with the TEMPO-2 result for minor non-disabling deficits; add OPTIMAS to `afib-anticoag-timing` references (cited in body, missing from frontmatter) and label its SVG timeline as a practice hybrid rather than "ELAN criteria"; fix the alcohol/"hemorrhagic transformation" wording in `metabolic-stroke-prevention`; reconcile the LDL &lt;55 vs &lt;70 targets across modules (state which patients get which); sweep all modules whose references list trials the body never mentions (CHANCE-3, ARAMIS, SPS3, MATCH in `dapt-regimens`; ATLAS/MR CLEAN-LATE in `large-core-thrombectomy`) and integrate or prune.
11. **Trials database (Evidence Atlas → `content/trials/`)**: create an early-window EVT topic and add MR CLEAN, ESCAPE, EXTEND-IA, SWIFT PRIME, REVASCAT, HERMES; add the orphan-citation trials that already have citation records (SAMMPRIS, TESLA, FASTEST, DECIMAL/DESTINY/HAMLET/DESTINY-II, ANNEXA-4, ACTION-CVT, BASICS, AXIOMATIC-SSP); re-file ARTESIA under subclinical-AF and OCEANIC-STROKE under factor-XI (or add cross-topic membership); fix CREST-2 `year` (published online NEJM 2025-11-21 — verify print vs online convention); verify CHOICE-2 n (433 analyzed vs 440 randomized) and THEIA's teaching point (currently warmer than its recorded neutral finding); refresh `activeTrials.js` per-record `lastReviewed` (all stamped 2026-04-25) and consider a CI staleness gate for registry-verified records &gt;90 days old.

## 4. P2 — internal consistency (make the app agree with itself)

1. **Post-EVT BP** stated four ways (`bp_post_evt` "&lt;180/105" no floor; `bpPhaseTargets['post-evt']` 140–180; `bp_post_evt_drip` "floor ~130"; `DOC_TEMPLATES.postEvt` "140-180 ≥72 h") and three of them fire on `evtRecommended` alone, i.e. **before the procedure**. Standardize on one mTICI-gated scheme (only `bpPhaseTargets` gates on documented mTICI today).
2. **DOAC restart timing** stated four ways (two GUIDELINE_RECOMMENDATIONS rules, `CLINICAL_PEARLS.doac_timing`, and `DOAC_PROTOCOLS` `elan-optimas` default in `src/calculators.js`). Standardize on the ELAN/OPTIMAS/CATALYST scheme the app already declares as default and delete or align the rest.
3. **Fever threshold** (&gt;38.0 vs &gt;37.5 — both cards render together), **LDL target** (&lt;70 vs &lt;55 rules both fire), **secondary-prevention BP** (&lt;130/80 vs &lt;140/90) — one rule each, or explicit patient-selection language; attribute &lt;130/80 to the 2025 AHA/ACC hypertension guideline.
4. **Window banner vs extended-window rules**: `getWindowStatusFromTime` announces "TNK window closed" at 4.6 h while `tnk_extended_imaging` and the auto-engine offer imaging-gated IVT to 9 h; make the banner say "standard window closed — imaging-based selection possible to 9/24 h."
5. **Contradictory MeVO output**: `transfer_evt` (regex matches `mca` inside "M2 MCA branch") and `mevo_evt_not_recommended` (matches `m2`) fire together. Tighten the vessel regexes and add M2-dominance vocabulary (also needed by the trials matcher, P3-1).
6. **Duplicate/near-duplicate rule pairs** (dysphagia ×2, VTE ×3, basilar ×2, carotid ×2, ICAS ×2, AF timing ×2, DAPT/TIA ×2, craniectomy ×2): merge each pair, keeping the better-cited version.
7. **Fragile `conditions()` predicates**: replace substring matching (`pmh.includes('af')` matches "graft"; `hx.includes('mi')` matches "mild"; `toast.includes('large artery')` fires for extracranial disease; `toast === ''` gives every un-TOASTed patient the LDL &lt;55 card) with word-boundary regexes or structured fields; fix the ~15 rules using `parseInt(nihss)||0` so a blank NIHSS no longer means 0 (currently every patient with no NIHSS gets minor-stroke DAPT advice); `tia_dapt` needs its stated high-risk gate (ABCD² ≥4); `evt_basilar`'s NIHSS 6–9 branch is unreachable (`conditions` requires ≥10); `decompressive_craniectomy`'s age &gt;60 text is unreachable (conditions cap at 60); `neurosurgery_communication` shows ICH text for ischemic NIHSS ≥15; `bridging_ivt_evt` says "Do NOT withhold IVT" even when the TNK auto-block is active — gate it.
8. **Two incompatible DOAC-recency checks** (flat &lt;48 h flag vs CrCl-adjusted clearance model) can flag and clear the same patient on one screen — keep the CrCl-adjusted model, make the banner read from it; stop hard-blocking TNK solely because last-dose time is undocumented *(judgment — document whichever gate you keep)*.
9. **Trials-tab compliance framing**: the banner calls real registry records "synthetic" — reword to "Real ClinicalTrials.gov records, first-pass extraction — not a complete protocol; not for clinical decision-making"; move the banner above the results; append the compliance note to the eligibility tables' copy-as-HTML/Markdown exports (`buildHtml`/`buildMarkdown` currently strip it) and render the exported-but-never-imported `ELIGIBILITY_COMPLIANCE_NOTE`; add the same first-pass caveat to the Encounter-tab auto-matcher and change its note output from `ELIGIBLE:` to `TRIAL SCREEN (first-pass, unverified):`.

## 5. P3 — engine/logic defects

1. **Matcher engine** (`src/evidence/matcher-engine.js`): `domainMatch` must return `null` (needs-info) when either NIHSS or the occlusion list is un-entered (currently a definite `'none'` flips STEP to not-eligible on a fresh form); add M2/M3 dominance to the vessel vocabulary (STEP wants non-dominant M2, TESTED wants dominant — one bare `'M2'` token serves both); replace the negation-blind `present` substring operator ("No intracranial stenosis" currently **matches** CAPTIVA's stenosis criterion; "No tandem lesion" matches PICASSO) with negation-aware or structured matching; implement STEP's MRI ASPECTS &lt;7 threshold (only CT &lt;6 exists).
2. **Screener** (`src/evidence/screener-eval.js`): the "enrolling soon" branch is dead code (early `return {status:'excluded'}` precedes the `isSoon` computation) — hoist it so a day-2 ICH+AF patient surfaces as a future ASPIRE match instead of "excluded", and drive it from each trial's declared window rather than hardcoded acronyms; note `eligible` is unreachable by design (all trials are first-pass) — either remove the dead green "Candidate" state from the UI or document it; fix the Hemorrhage preset (`onsetVal: 3` + forced `'hours'` matches no preset button); delete the stale `src/evidence/screener/` mirror (missing SATURN) or regenerate it.
3. **Ten calculator cards collect inputs and render no result** (CrCl, enoxaparin, RoPE — titled "and PASCAL" but shows neither — HAS-BLED, RCVS², PHASES; plus frozen FUNC/SPAN-100+SEDAN/DRAGON/mTICI). Wire the six non-frozen ones to their existing compute functions (`calculateCrCl`, `calculateEnoxaparinDose`, `calculateROPEScore` + `evaluatePASCAL` + the Kent attributable-fraction table, `calculateHASBLEDScore`, `calculateRCVS2Score`, `calculatePHASESScore`+`getPHASESRisk`). This changes `calculators.txt` — separate re-baseline commit per rule 0a. Add a one-line PMID citation to every calculator card (currently zero citations render in the whole panel) and a registry↔DOM test asserting every `registry.json` id has a matching card.
4. **`GUIDELINE_RECOMMENDATIONS` infrastructure**: `MANAGEMENT_REC_TO_ATLAS_REC` keys don't match real rule ids (only 5/11 resolve — the "Why this recommendation?" drawer never appears for reperfusion rules); add `lastReviewed` + working `sourceUrl` to all ~126 rules and surface staleness; `GUIDELINE_CLASS_COLORS` needs entries (or normalization) for `'N/A'`, composite classes, and a distinct color for III: Harm vs III: No Benefit (currently identical red, and the guidelines library encodes harm only in `classNote`).
5. **Encounter form plumbing**: video-telestroke branch has no Disposition control (readiness can never hit 100%; GWTG STK-5/6 warnings and the note emitter read it), no structured CTP inputs (extended-window auto-gate can never fire in video mode), and dead jump targets for Weight/Diagnosis; six DOM ids are duplicated across the phone/video branches and the `phone-` id prefix lives on the video branch — rename coherently; `encounterOutputGate` is inert (`required` hardcoded `[]`) — populate it or remove the module; fix the `clearAllAndProceed` TDZ hazard (reads consts declared below it).

## 6. P4 — structural, dead code, docs

1. What's New feed is generated, validated, precached, and **never rendered** — `whats-new.json` is imported then tree-shaken, and routes `#/research/whats-new|whatsnew` map to a subtab id that matches no panel, rendering an **empty Research tab**. Either build the panel or remove the import + routes; also refresh the briefing pipeline (verified-pmids stale since 2026-05-30; `whats-new-source-gaps.md` still lists the deliberately-removed SISTER trial).
2. Guidelines UI: move the searchable 861-rec Guideline Library (`<details id="ref-guidelines">`, currently collapsed inside the **References** sub-tab) into the Guidelines sub-tab, which today is only an 88-item external-link grid; render long `classNote` text as a footnote instead of inside the COR badge (stub datasets currently produce ~115–250-char badges); add per-recommendation anchors and a PubMed fallback link beside the paywalled `ahajournals.org` PDF deep links; mark the 66 abstract-only stub datasets "summary only — see source" so empty search results aren't read as guideline silence.
3. Eight education PDF links 404 (no file on disk, no generator): `Fibromuscular Dysplasia`, `Antiepileptic Drugs`, `CADASIL_CARASIL_Vasculopathies`, `Moyamoya_Disease_Surgical_Revascularization`, `Cancer_Associated_Stroke_NBTE`, `DMVO_MeVO_Management`, `ICH_Blood_Pressure_Expansion`, `Metabolic_Stroke_Prevention` under `documents/references/` — generate via `scripts/generate-pdfs.mjs` or remove the `pdfPath` props; add an existence test for `education.jsx` pdfPath props (the current dead-link test only walks `bundle.references`).
4. References tab: add `year`/`supersededBy` to the reference schema and date badges in the UI (several undated pre-2024 PDFs — the MeVO trials deck, the July-2024 AF deck, the 2022 lacunar deck — now sit beside newer contradicting evidence); register the ~6 rendered-but-unregistered documents/links; add the missing `ref-code-stroke` TOC anchor.
5. Education housekeeping: dead filter chips (`epic`, `trials`, `needs-review` always empty), orphaned `telestroke-map` render case with a hardcoded personal external URL, silent fall-through for unknown education deep-links, `'nursing'`→`'quality'` mapping, absent `keyTakeaways` across all 40 modules (add to schema + files or drop the expectation).
6. Settings/misc: dangling `aria-labelledby="tab-settings"`; dead Contact Directory code (comment promises a UI that doesn't exist); `window.strokeP0.version` stuck at `'5.20.0'`; in-app `CHANGELOG` array ends at v3.4 and is never rendered — delete or wire; Save/Cancel both dump to Encounter; README claims install lives in Settings but `InstallAppButton` renders only in the Trials header.
7. `npm run lint:tokens` fails today (9 raw-palette violations — one at the frozen-zone line ≈29291 is className-only and snapshot-safe); fix and consider wiring `lint:tokens`/`lint:contrast` into CI.
8. Process guards worth adding while you're here: orphan-citation report in `scripts/evidence-validate.mjs`; reference-vs-body drift test for education modules; extend `check-currency.mjs` to walk `src/guidelines/` (the 861-rec library currently has no `lastReviewed` and is invisible to the currency gate); wire `content:validate`/`content:bundle:check`/`agent:assets:check` into CI (none run in workflows today).
9. Update stale doc claims as you finish: `PROJECT.md`/`README.md` "17 guideline datasets (771 statements)" → real counts; the four disagreeing test-count claims (`CONTRIBUTING.md` 427, `README.md` 850, `vitest.config.js` ~472, `GATE_STATUS.md` 1,323); `REFACTOR_MAP.md` §7's obsolete frozen-zone description.
10. `CHANGELOG.md` is 9 minor versions behind `package.json` (last entry v6.11.5) — backfill a compact entry per shipped version, then your own.

## 7. P5 — content gaps (add as capacity allows, in this order)

1. **PFO / PFO closure module** — the single largest education gap: CLOSE/RESPECT/REDUCE/DEFENSE-PFO, RoPE mechanics, PASCAL, closure vs anticoagulation vs antiplatelet, post-closure AF (the repo already ships `eso-pfo-2024.json`).
2. Thrombolysis-associated ICH reversal section in `anticoagulation-reversal` (cryoprecipitate, TXA, fibrinogen target) + actual andexanet dosing regimens (low/high dose, &gt;18 h rule).
3. INTERACT4 cross-link in `ich-blood-pressure` (diagnosis-dependent prehospital BP answer); TCAR/near-occlusion/carotid web in the carotid module; SATURN (statins after ICH); LAA-occlusion education (LAAOS-4/OPTION context); factor-XI-inhibitor class primer; a stroke-mimics module; NIHSS UN options for items 5a/5b/6a/6b/7 (compute already handles UN).
4. Trials-tab coverage: eligibility-table rows for CAPTIVA/PICASSO/MOST-successors; the ischemic-acute table has a single row.

## 8. Needs-web-verification queue (check before or during the work)

PICASSO enrollment status (primary completion passed 2026-03-08) · CLARITY activation (listed start 2026-08) and its "Age ≥40" criterion · CREST-2 print-issue year · CHOICE-2 n=433 vs 440 · ENRICH-AF status · identity of "ZODIAC" as used in stroke positioning literature · LIBREXIA-STROKE status · the exact content of the 2026 AIS guideline erratum (PMID 42507797) · whether any AHA/ASA/ESO guideline released after 2026-08-22 supersedes anything above · "AAN 2022 (reaffirmed 2025)" currency claim · PRESTIGE-AF CIs · "STROKE-CLOSE 2024" citation.

## 9. Acceptance criteria

- `tests/snapshots/example-protocols/ich.txt` and `ischemic.txt` byte-identical to their pre-session state; `calculators.txt` re-baselined only in a dedicated commit if calculator wiring changed rendered text.
- All of: `npm run content:validate`, `content:seed:check`, `content:bundle:check`, `evidence:validate`, `validate:citations`, `validate:pmids`, `validate:inline-citations`, `validate:whats-new`, `check:leak-guard`, `npm run test:unit`, `npm run test:protocol-snapshot` pass; `npm test` passes.
- Every clinical change carries a verified PMID/DOI; every P0 item is either fixed or explicitly listed as blocked in the PR with the reason.
- Version bumped in all five lockstep locations + service-worker test expectations; rebuilt `app.js`/`tailwind.css`/`data/**`/`llms*.txt` committed; `CHANGELOG.md` updated.
- Draft PR opened with: summary by priority tier, "Decisions for physician review" section, verification sources for each clinical change, and a status list of deferred items.
