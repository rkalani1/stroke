config.local.json
node_modules/

# Local-only protocol extension layer. Never commit or deploy real local
# institutional protocol text, contacts, paths, or source excerpts.
private/
src/institutional-protocols.local.js
scripts/leak-guard-denylist.local.json

# Agent discovery / local-extract scratch. Contains protected source material
# used only to author the gitignored local extension layer. Keep local; never
# commit or publish.
.discovery/
.pw-audit.mjs
.pw-audit-report.json
.DS_Store
npm-debug.log
output/

# Pre-compressed build artifacts from scripts/compress-assets.mjs
*.gz
*.br

# Capacitor build artifacts. The scaffold (ios/, android/, capacitor.config.json)
# is committed; build outputs are not.
dist/
ios/App/Pods/
ios/App/Podfile.lock
ios/App/build/
ios/App/App/public/
android/app/build/
android/.gradle/
android/build/
android/local.properties
android/app/release/
android/app/src/main/assets/public/
---
# v7 worktree node_modules symlink
/node_modules

# v7 codemod backups (reproducible via re-running codemod-v7.mjs; idempotent)
*.codemod-backup.txt

# Claude / Antigravity subagent worktrees
.claude/

coverage/

# Session scratch/debug artifacts — never publish
scratch/

# Local dev conveniences — never publish
.claude/
.playwright-mcp/
